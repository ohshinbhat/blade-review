# Optional. Without it the agent runs at the deterministic floor (Layer 1 only).
ANTHROPIC_API_KEY=
BLADE_REVIEW_MODEL=claude-sonnet-4-5
# Eval gate: fail CI if false approves exceed this.
EVAL_FALSE_APPROVE_BUDGET=0
