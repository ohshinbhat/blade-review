import React from 'react';
import { Box, Typography } from '@razorpay/blade/components';

export const DemoUI = (): React.ReactElement => (
  <Box style={{ padding: 9 }}>
    <Typography>Off-scale inline spacing</Typography>
  </Box>
);
